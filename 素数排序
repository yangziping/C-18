#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
	int f(int n);
	int a[20],i,k,j,t,m;
	scanf("%d",&m);
	for(i=0;i<m;i++)
	scanf("%d",&a[i]);
	for(j=0,k=0;j<i;j++)
	{if(f(a[j])==1)
	{a[k]=a[j];
	k++;
	}
	}
	for(i=0;i<k-1;i++)
	{for(j=0;j<k-1-i;j++)
	{if(a[j]>a[j+1])
	{t=a[j];
	a[j]=a[j+1];
	a[j+1]=t;
	}
	}
	}
	for(i=0;i<k;i++)
	printf("%d ",a[i]);
	printf("\n");
	system("pause");
	return 0;
 } 
 
 int f(int n)
 {
 	int i,a;
 	for(i=2;i<=sqrt(n);i++)
 	if(n%i==0)
 	break;
 	if(i>sqrt(n)&&n!=1)
 	a=1;
 	else
 	a=0;
 	return(a);
 }
